import InDevSection from '../../components/InDevSection/InDevSection';
import '../NotFoundPage/NotFoundPage.scss'

const NotFoundPage = () => {
  return (
    <main className="page">
        <InDevSection sectionName='Not found'/>
    </main>
  )
}

export default NotFoundPage;